# dependencias
node_modules/

# salidas de la CLI
*-tracks/
*-tracks.zip

# servidor HTTPS local (certificado autofirmado)
.cert/

# muestras de sonido descargadas con `npm run fetch-sounds` (pesadas)
web/soundfonts/

# ajustes locales de Claude Code
.claude/settings.local.json

# fotogramas intermedios del GIF (se regeneran con `npm run capture`)
docs/frames/

# varios
.DS_Store
*.log
